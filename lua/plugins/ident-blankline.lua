-- This plugin adds visible indentation guides to all lines in Neovim using virtual text .

return {
    "lukas-reineke/indent-blankline.nvim",
    main = "ibl",
    ---@module "ibl"
    ---@type ibl.config
    opts = {},
}
